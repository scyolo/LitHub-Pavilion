# 《系统设计方案》v1.0 设计审查报告

> 审查日期：2026-09-18 · 审查对象：`系统设计方案.md` v1.0 → 修订为 v1.1
> 审查方法：五个维度加载专项审查框架（api-design / backend-patterns / security-review / docker-patterns / frontend-patterns），并对**所有依赖外部 API 的关键假设做联网实测**（不靠记忆下结论）。

## 一、实证核对结果（审查的证据基础）

| 验证项 | 方法 | 结果 | 对设计的影响 |
|---|---|---|---|
| DBLP search API 可用性 | curl 直连 dblp.org 与镜像 dblp.uni-trier.de，分别用无 UA / Chrome / httpx / requests 四种 UA | **全部返回 HTTP 200 + Anubis 反爬质询页**（`Making sure you're not a bot!`，含 `/.within.website` 标记）；XML dump 端点同样被质询 | 原设计"每周从 DBLP TOC 采集"通道当前**不可用**，属 P0 级设计缺陷（风险表原判"概率低"错误） |
| OpenAlex 可用性 | curl 直连 works 端点 | 返回**预算/积分制**错误（`Insufficient budget… Resets at midnight UTC`，每日配额） | 原"10 req/s polite pool 无限兜底"假设失效，需预算感知节流 |
| Semantic Scholar 限速 | 官网文档 | 官方原文：有 key **1 RPS**；无 key 1000 RPS 共享池（不稳） | 原文"免费档 1 req/s"表述不准；无 key 通道按 0.5 RPS 保守限速 |
| S2 batch 接口 | 无 key POST 实测 2 个 arXiv ID | **200 可用**，且 `externalIds` 直接返回 DBLP key / DOI / ArXiv ID | 500 篇/请求成立；`externalIds.DBLP` 可用作 OpenAlex→DBLP 归并键 |
| 期刊按年采集 | DBLP 组织结构分析 | 期刊按**卷（volume）**组织、无年度 TOC；`toc:{stream}{year}.bht` 仅适用于会议 | 原"期刊也走 DBLP TOC"不可实现，期刊采集须走 OpenAlex ISSN+年份过滤 |

## 二、发现清单（按严重度）

| # | 严重度 | 审查维度 | 问题 | 修复（落点） |
|---|---|---|---|---|
| R1 | **P0 · 已发生** | 数据源实证 | DBLP 全通道被 Anubis 质询，采集主链路不可用，原风险表低估 | 新增"通道探测与主备切换"机制：每周期探测、质询页识别规则、自动切 OpenAlex、`DBLP_BASE_URL` 可配置回切；不实现质询绕过（4.1） |
| R2 | **P0** | 数据源实证 | 期刊无法按 DBLP 按年采集 | 期刊固定 OpenAlex（ISSN+`publication_year`）；会议保留 DBLP TOC；`venues` 增加 `dblp_toc_pattern`（如 `nips{year}`）消除卷名猜测（4.2、5.2） |
| R3 | **P0** | 安全 (security-review) | SSRF：S2/OpenAlex 返回的 `openAccessPdf.url` 是外部可控 URL，原设计直接下载 | 6.4 新增四重校验：https-only、域白名单、DNS 解析后拒绝私网/保留 IP、禁自动重定向并逐跳复检（6.4、2.2） |
| R4 | **P0** | 安全 (security-review) | 路径穿越：`pdf_path` 若被污染可越出 `PAPERS_ROOT` | PDF 服务前 `resolve()` + `is_relative_to` 校验（6.1 代码、6.4） |
| R5 | **P1** | 后端 (backend-patterns) | APScheduler 在 uvicorn 多 worker 下产生多个调度器实例 → 重复采集 | 明确 `--workers 1`、禁 `--reload`，写入部署与调度两处（4.1、7.1） |
| R6 | **P1** | 数据源实证 | OpenAlex 预算制；原 10 req/s 假设失效 | ≤2 rps + mailto + 预算耗尽暂停至 UTC 午夜重置（2.2、4.1） |
| R7 | **P1** | API (api-design) | 错误体与 FastAPI 默认 `{"detail":…}` 不一致，将出现两套格式 | 全局 exception handler 覆盖为统一 `{"error":{code,message}}`（4.4） |
| R8 | **P1** | API (api-design) | POST 建资源缺 201/Location；416 缺 `Content-Range: bytes */size` | POST /api/papers 改 201+Location；416 补规范头（4.4、6.1） |
| R9 | **P1** | 前端 (frontend-patterns) | "react-pdf（pdfjs-dist 3.x）"与 worker 路径 `.mjs` 互相矛盾（3.x 无 .mjs worker） | 修订为 react-pdf v9 + pdfjs-dist 4.x，注明版本配套约束（4.5） |
| R10 | **P1** | 数据模型 | OpenAlex 成为运行时主源后，`dblp_key NOT NULL` 与单源假设不再成立 | `papers` 增加 `source`/`openalex_id`（表级 CHECK 保证两源一致性），`dblp_key` 改 NULL；去重规则 2 升级为跨源 DOI 归并键（5.2、4.2） |
| R11 | **P1** | 部署 (docker-patterns) | 备份依赖宿主机 sqlite3 CLI（Windows/精简镜像常缺失，静默失败） | 改 `python -m scripts.backup`（`Connection.backup` API），清理策略按 `-mtime +27` 跨平台一致（7.2） |
| R12 | **P1** | 部署 (docker-patterns) | 容器未做非 root / 提权防护 | Dockerfile USER appuser + `no-new-privileges:true`（6.4、7.1） |
| R13 | **P2** | API 一致性 | `/api/crawl/reclassify` 在 4.5 出现但 4.4 表缺失；`oa=` 参数名与 `pdf_status` 枚举不一致 | 补表行；参数统一为 `pdf_status=`（4.4） |
| R14 | **P2** | 后端 (backend-patterns) | 列表接口存在 N+1 风险（逐行取方向标签/作者） | 明确单条 SQL：JOIN + `GROUP_CONCAT` 聚合 + 子查询取前 3 作者（4.4） |
| R15 | **P2** | 前端 (frontend-patterns) | 搜索无防抖；PDF.js worker 失败无降级 | 输入 500ms 防抖；预览外包 ErrorBoundary 降级为"新窗口打开"（4.5） |
| R16 | **P2** | 容量/估算 | "首次回填 10–20h"错误（按单篇估算，忽略 batch）；瓶颈实际在 PDF 下载 | 元数据 ≈1h、PDF 12–17h 由每日 backlog 分摊 2–4 天（A11、1.2） |

## 三、审查后仍接受的取舍（不算缺陷）

1. **不做 Anubis 质询绕过**——技术上 metarefresh 质询可被简单客户端跟随，但绕过反爬控制脆弱且不合规，采用"探测—切换—自动恢复"。
2. **无 `/api/v1` 版本前缀**——内部单人 API 无版本共存需求，已在 4.4 注明理由。
3. **authors.name_norm UNIQUE 可能合并同名学者**——系统无作者维度功能（A2），合并只影响人名去重显示，不影响论文数据。
4. **APScheduler 内存 jobstore**——单进程常驻 + `misfire_grace_time=86400` 已覆盖重启场景，持久化 jobstore 引入复杂度不成比例。

## 四、结论

v1.0 的结构性设计（架构分层、FTS5、多对多模型、断点续传、目录归档）经五维审查**成立**；但**数据源可达性假设整体过时**（DBLP 反爬、OpenAlex 预算制，均为 2026 年实际状态），以及安全（SSRF/路径穿越）与调度单实例两处硬伤。v1.1 已将全部 16 项发现落进文档对应章节，新增 `venues`/`papers` 字段与通道探测机制后，第 2 周里程碑验收标准同步加入了"模拟质询 → 自动切 OpenAlex"的可执行验收项。

---

## 五、迭代审查记录（v1.1 → v1.2，2026-09-19，三轮）

本轮按用户要求聚焦：①顶会顶刊论文的**正确性与时效性**；②**闭源论文尽力显示 PDF、实在不行回退官方链接**；③整体可行性。每轮均先找证据再改设计。

### 迭代二 · 论文正确性与时效性

| # | 严重度 | 问题 | 证据/推理 | 修复（落点） |
|---|---|---|---|---|
| I-1 | P0 | OpenAlex 通道的会议论文 `primary_location` 常指向其 **arXiv 版**（source=CoRR），按 primary_location 归属会把 venue 记成 arXiv、年份记成预印本年，归档目录和年份统计全错 | OpenAlex 对 arXiv/正式版双位置的论文普遍以 CoRR 为 primary_location 的已知行为；DBLP 通道被质询期间此问题必现 | S2 补全返回的 `externalIds.DBLP`（stream 前缀+年份段）作为 venue/年份**权威源**，命中置 `venue_confirmed=1`；未命中才回退 OpenAlex 并置 0；DBLP 恢复后按 DOI 回填翻转（4.2）；新增 F-020 抽样验收 ≥98% |
| I-2 | P1 | Workshop/副轨论文可能混入主会归档 | DBLP workshop 是独立 TOC 卷、OpenAlex workshop 是独立 source | 显式声明双通道天然排除机制 + 第 2 周验收"NeurIPS 2024 入库清单不含 workshop 卷"（4.2、§9） |
| I-3 | P1 | 时效性：8–12 月 AI 会议 proceedings 集中发布，每周一次的采集会形成最长 ~10 天的可见延迟 | EMNLP(11 月)/NeurIPS(12 月)/AAAI 等发布窗口集中 | 新增 `weekly_crawl_peak`（8–12 月周四 04:00 加跑）+ `monthly_metrics` 月度引用刷新（4.1、F-021/F-022） |
| I-4 | P1 | 时效性无法度量=无法保障 | 原设计只有"跑没跑"，没有"滞后多少" | 新增 `papers.publication_date` 列 + 收录滞后监控：中位滞后 >21 天 WARNING、连续两周期升级告警（4.1、5.2） |

### 迭代三 · 闭源论文 PDF 显示策略（版权合规的多渠道解析）

用户需求变更：闭源论文"尝试显示 PDF，实在不行显示官方链接"。设计原则：**只使用出版方/会议主办方自有的开放渠道，绝不触碰付费墙**——这既满足"尽力显示"，又不越过版权红线（各主办方 OA 库与 arXiv 同属官方开放分发，合法性等价）。

| # | 严重度 | 问题/变更 | 证据（2026-09-19 实测） | 修复（落点） |
|---|---|---|---|---|
| I-5 | P0 | 原设计"仅从 arXiv 下载"使大量有官方 OA 版的论文被判"闭源"（ACL Anthology、CVF、OpenReview、PMLR、AAAI OJS、IJCAI 均为全量 OA） | 白名单 9 组 host 可达性实测：aclanthology.org/proceedings.mlr.press/openaccess.thecvf.com/openreview.net/ojs.aaai.org/ijcai.org/jmlr.org 均 200；**proceedings.neurips.cc 与 papers.nips.cc 本网络连接失败**；direct.mit.edu（MIT Press）对非浏览器 UA 返回 403 | PDF 解析链改为"多渠道候选 + 域白名单 + 按优先级依次尝试 + **主机级熔断**（失败 host 30 分钟内跳过）"；白名单表 `pdf_whitelist` 落地（4.1）；NeurIPS 不可达时自动降级 OpenReview/arXiv，MIT Press 仅作外链展示 |
| I-6 | P1 | "closed"一刀切，丢失"发现了合法 OA 链接但不在白名单/下载失败"的信息 | — | `papers` 新增 `pdf_source`（实际下载渠道）与 `oa_url`（白名单外合法 OA 外链）列；`closed` 分两态：有 `oa_url` → "开放版本外链"按钮，无 → DOI 官方链接按钮（4.4、4.5、5.2） |
| I-7 | P1 | 非.arXiv 渠道的"同篇校验"未覆盖 | Anthology/CVF/OpenReview 的 PDF 与 DBLP 记录需要防错配 | 下载前归一化标题比对（相等或编辑距离 ≥0.95）+ 首作者姓一致，不适配即放弃该候选（4.1） |
| I-8 | 红线声明 | 版权边界必须显式可审计 | — | A4 重写：付费墙域名（IEEE/ACM/Springer/Elsevier）永不入白名单、不使用盗版镜像；白名单以 CSV+表形式维护可人工审核（0、4.1） |

效果估计：会议论文经主办方 OA 库覆盖率 ~95%+（Anthology/CVF/OpenReview/PMLR/AAAI/IJCAI 全量 OA），期刊中 JMLR/TACL/CL 全量 OA、TPAMI 类依赖 arXiv 预印本（50–70%）→ 整体 OA 覆盖率 **~90%**，"只能跳转"的论文大幅减少，且每一篇都有三级降级展示（内嵌 PDF → 开放版本外链 → 官方链接，F-019）。

### 迭代四 · 工程可行性与运维

| # | 严重度 | 问题 | 修复（落点） |
|---|---|---|---|
| I-9 | P1 | 下载器直接写目标文件 → 备份/读端可能读到半成品 PDF | `{filename}.part` 临时名 + `os.rename` 原子改名（4.3、6.3） |
| I-10 | P1 | SQLite 未定 PRAGMA；长事务会阻塞 API 读 | WAL + `synchronous=NORMAL` + `busy_timeout=5000` + `foreign_keys=ON`，批量 upsert 每 500 行一事务（6.3） |
| I-11 | P1 | 通道中途切换（DBLP→OpenAlex）与断点续传的交互未定义，可能重复或漏采 | 切换时受影响 venue×年份单元按"重跑"处理，归并键（dblp_key/doi/openalex_id）幂等保证 total 不变（6.3） |
| I-12 | P2 | FTS 触发器漂移（版本升级/迁移）无修复手段 | `python -m scripts.rebuild_fts` 重建+integrity-check 自检；`/api/health` 增加通道健康（DBLP 可用性/OpenAlex 预算/坏 host 状态）（6.3） |

### 迭代后结论

v1.2 在保持版权红线的前提下，把"闭源论文"的处理从"只能跳转"升级为**多渠道 OA 解析链 + 三级展示降级**，并补齐了论文正确性（venue 归属权威源、workshop 排除、抽样审计）与时效性（高峰期双频采集、滞后度量与告警）的完整闭环。遗留观察项（不影响开工）：NeurIPS 官方论文集在本网络环境不可达属环境相关现象，候选链已兜底；若后续发现 S2 对某主办方 OA 库的索引覆盖率偏低，可在 `pdf_whitelist` 增补 host 并为该 venue 增加定向解析规则（均为数据/配置级变更，无需改架构）。

---

## 六、迭代五记录（v1.3→v1.4，2026-09-19，整体优化：代码架构重构 + 文档主线切换）

用户决策：切换链接模式（不下载 PDF）后，对项目做从头到尾的整体优化，按设计文档重点审查代码设计问题。

### 审查发现与处置

**文档↔代码偏差（3 处，均已修）**

| # | 问题 | 处置 |
|---|---|---|
| D1 | v1.3 只改了假设/F-005/F-019，正文 1.2 目标、F4、3.2 数据流图、4.1 调度与解析链、A11、9 里程碑仍以"每日下载"为主线，与代码行为（链接模式默认）矛盾 | 全文重写为链接模式主线；下载内容收拢为"可选下载模式"标注节（4.3/6.1/7.1/里程碑第 3-4 周验收同步改） |
| D2 | F-008"PDF 在线预览"仍标 P0 | 改 P1 并注明仅下载模式启用 |
| D3 | A11 的 S2 行为描述缺"整批未知 → 400 No valid paper ids"实测结论 | 补充（该语义已在代码中正确处理为全 None 不重试） |

**代码设计问题（9 项，全部处置）**

| # | 问题（证据） | 处置 | 收益 |
|---|---|---|---|
| C1 | pipeline.py 811 行职责过载：编排+补全+打标+告警+断点+S2 bulk 组装 | 拆出 `services/enrichment.py`（补全/oa_url 写入）；S2 bulk 组装移入 `collectors/s2.py::fetch_bulk_raw_papers` | pipeline 702 行，采集层/服务层边界清晰，enrichment 可被月度刷新/链接回填复用 |
| C2 | oa_url 写入散布 4 处（upsert×2、apply_s2_record、pdf_backlog），语义各自实现 | 统一 `set_oa_url_if_empty()`（enrichment 唯一写入口，先到先得不覆盖），4 处调用 | 链接模式核心字段的写入语义单点可审计 |
| C3 | stats/taxonomies 把全表行载入 Python 计数（1.8 万行×每次请求） | 全部改 `GROUP BY + func.count()`；stats 新增 `with_oa_link`（链接模式核心指标） | 列表页/首页统计从 O(n) 行载入降为聚合查询 |
| C4 | list_papers/search 在 `Paper.venue lazy="joined"` 已加载的情况下二次查 Venue | 直接用 `p.venue.abbr`，删除二次查询 | 每页少 1 次查询、count 子查询更小 |
| C5 | links_backfill 尾部"剩余"计算错误（`len(id_list)+len(chunk)`） | 改为重新 count 查询 | 运维日志不再误导 |
| C6 | 死代码：`db.reset_engine_for_tests`、`openalex._COAR`、未用 import（re/Venue） | 全部删除 | - |
| C7 | config 模块 import 期副作用改 `settings.s2_rps` | 改 `model_validator`（有 key 且未显式配置时升 1.0） | 配置语义显式化，无隐藏副作用 |
| C8 | `_upsert_authors` 每作者一次 SELECT（500 篇×5 作者≈2500 查询/批） | 批内 `{name_norm: Author}` 缓存，经 `upsert_paper(author_cache=...)` 贯通 | 批量入库查询数降一个量级 |
| C9 | 测试缺口：链接模式行为无覆盖 | 新增 `test_link_mode.py` 6 项：新论文 closed+oa_url、下载模式 pending、pending 自愈、oa_url 先到先得、`_best_oa` 提取、GROUP BY 计数一致性 | 48 项测试全绿 |

### 验证

- 48 项 pytest 全绿（新增 6 项链接模式专项）。
- 重启服务后真实网络全链路冒烟：stats（GROUP BY，total 18,238 / with_oa_link 7,719）、检索（joined venue 复用，venue 显示正确）、方向计数（llm 2792 / agent 790 / specdec 19）、weekly 采集（通道探测 anubis→OpenAlex 降级→ICML S2 bulk 兜底 1→8 篇）全部正常。
- 期间顺手修复重构引入的 2 处机械损伤（openalex 相邻行粘连、pipeline 类锚点误删），均被 import/pytest 即时捕获。

---

## 七、迭代六记录（2026-09-19，冗余清理专项）

工具化检测（ruff F401/F811/F841/E731）+ 人工排查，全部处置：

| # | 冗余 | 处置 |
|---|---|---|
| R1 | 18 处未用导入（ruff 自动修复：deps/papers/taxonomies/pdf_downloader/db/models/links_backfill/pipeline ×2/tests ×5） | `--fix` 全清，测试保持全绿 |
| R2 | `deps.get_pipeline` 死函数（路由均直接用 `app.state.pipeline`） | 删除，deps 收敛为单一 `get_db` |
| R3 | `_ENRICH_BATCH_SIZE=500` 在 pipeline 与 enrichment 重复定义 | pipeline 改为从 enrichment 导入（单一来源防漂移） |
| R4 | 前端 `PaperList.jsx` 残留 `useMemo` 导入（防抖重写后未用） | 删除 |
| R5 | 前端 `api.patchPaper` 客户端函数无任何调用方 | 删除（后端 PATCH 端点保留，属 P2 API 面） |
| R6 | `papers.py` 与 `search.py` 各自维护方向聚合/首作者查询/卡片序列化（~80 行重复） | 抽取 `app/api/serializers.py`（directions_for / authors_for / paper_card），两路由共用；检索结果在卡片上附加 bm25 score |

验证：ruff 全过、48 项 pytest 全绿、前端构建通过（主包 221kB/PDF 374kB 分包不变）、经 5180 站点冒烟（列表首作者+作者数、检索 score、统计 with_oa_link=7,774）输出与重构前一致。期间一次 import 删除过窄（详情端点仍需 Author/PaperAuthor）被测试即时捕获修正。

---

## 九、迭代七/八记录（2026-09-19，用户要求的两次迭代审查）

### 迭代七 · 前端与文档一致性（审一轮）

| # | 问题 | 处置 |
|---|---|---|
| V1 | 首页统计卡片在链接模式下永远显示 0："已归档 PDF"（`pdf_archived` 恒 0）与"OA 归档率"（恒 0%）误导用户 | 改为"带开放获取链接"（`with_oa_link`）与"全文链接覆盖率"，语义与链接模式一致 |
| V2 | 设计文档 §8 风险表 3 行残留下载时代措辞（"归档目录错"/"PDF 落目录一次定型"/"三级展示：内嵌 PDF→…"），且缺链接模式特有风险 | 重写为链接模式措辞；新增"OA 链接覆盖率爬坡慢（每周二回填）"风险行 |

### 迭代八 · 数据质量与采集覆盖实测（审二轮，全部实测驱动）

审计发现 4 个真实缺陷，全部修复并验证：

| # | 问题（证据） | 处置 | 效果 |
|---|---|---|---|
| Q1 | **期刊卷首噪声入库**：OpenAlex 把 "Editorial Board"/"Information for Authors" 等当 work 收录，实测 96 条混入（PR/TNNLS 等，"期刊名+栏目名"形态） | `cleaning.is_noise_title()`（精确集 + 后缀匹配）；入库批处理与 S2 bulk 结果双过滤；存量清理 96 条；2 项新测试 | 库内噪声清零，后续不再进入 |
| Q2 | **431 条空串 oa_url**：构造路径 `oa_url=raw.extra.get("oa_pdf")` 绕过空值检查（OpenAlex 偶返 ""） | 入库与回填两处统一 `or None` 归一；存量置空；新测试覆盖 | 不再出现无 host 的"链接" |
| Q3 | **8 个 venue 采集为 0 的三重根因**：① OpenAlex source 名称解析失败时 `raise` 杀死单元，跳过本可救回的 S2 bulk（NeurIPS/ACL/CVPR 的 OpenAlex 解析失败，但 S2 有货 1788/2444/369）；② 预算熔断在函数入口，把不耗 OpenAlex 预算的 S2 bulk 也拦死；③ S2 bulk 宽查询为 AND 语义，AAMAS 等不含全部词的会议返回 0（单词 "agent" 实测 259 篇） | ① 解析失败/预算耗尽一律降级进 S2 bulk；② 熔断仅作用于 OpenAlex 取数段；③ 改 7 个单查询词分别拉取、按 DBLP key 去重合并（`s2_bulk_queries`）；补 AAMAS/UAI/KR/ECML-PKDD 的 `s2_venue`（DB+CSV） | NeurIPS 0→2,328、ACL 0→1,673、CVPR 0→257、UAI/KR 救回（KR 60）；总量 18,238→23,170 |
| Q4 | 修复③引入的缩进错位（`works` 未绑定 + 已关闭 client 使用），日志即时暴露 | 修正缩进 | `UnboundLocalError`/`client closed` 两类单元失败归零 |

**遗留说明**（非缺陷）：ECAI 双年会（2025/2026 无卷），0 属正常；TASLP 期刊依赖 OpenAlex 预算，本轮耗尽暂停、下周自动恢复；ECML-PKDD 在 S2 未按该缩写索引，需人工确认有效 venue 名（venues.csv 单行修正即可）。FTS integrity-check 通过（papers=fts 行数一致）、年份无离群、venue_confirmed 66%（随 DBLP 恢复上升）。

测试：50 项全绿（新增噪声过滤/空串归一 2 类）；ruff 零告警；采集运行中多查询兜底持续填充（with_oa_link 47% 且上升）。
