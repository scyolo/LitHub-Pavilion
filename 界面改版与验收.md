# PaperTracker 界面改版与验收

> 日期：2026-09-20。交付：参考 CCF 论文雷达的信息组织方式，为现有本地应用完成研究工作台改版。运行入口：http://127.0.0.1:5180/ 。

## 1. 参考与取舍

已用浏览器实际查看 [CCF 论文雷达](https://szy12021130.github.io/ccf-a-radar/#/) 的总览与会议详情，借鉴以下交互结构，而非照搬其数据、品牌或会议名单：

- A/B 会议、A/B 期刊的快捷切换。
- 真实统计总览、年度分布、主题分布，以及从图表深入论文列表。
- 一致的论文信息层级：来源和级别、标题、作者、主题、原文链接。
- 来源卡片和年度概况，便于发现值得跟踪的研究社区。

使用独立的深色研究工作台视觉系统，并支持浅色主题。保留 FastAPI + SQLite FTS5 + React/Vite；不引入外部图表服务。图表采用 SVG 和原生可访问按钮。

## 2. 已落地界面

| 页面 | 路径 | 功能 |
|---|---|---|
| 研究总览 | `/` | 本地统计、A/B 年度柱状图、主题条形图、开放链接占比、主题入口、最近收录和来源数量排名 |
| 论文探索 | `/papers` | 快捷范围切换、方向多选、年份/来源/开放链接组合筛选、450ms 防抖英文检索、排序、分页、舒适/紧凑显示 |
| 论文详情 | `/papers/:id` | 完整摘要、作者与元数据、官方/OA外链、归属提示、复制引用，返回列表保留筛选 |
| 会议与期刊 | `/venues` | 来源搜索、收录量/名称排序、来源卡片和年度微型图，零数据不推断未发表 |
| 采集管理 | `/admin` | 实际运行状态、下次调度、覆盖情况、分页历史日志、显式元数据同步及回填确认 |

全局包含折叠式手机导航、主题切换、键盘焦点、跳转主要内容链接、减少动态效果偏好。没有 PDF 下载或预览入口。

## 3. 方向与数据保护

原有 LLM、投机解码、智能体保留；只在既有 A/B 来源内新增以下细粒度方向：

| 新方向 | 本次新增标签数 |
|---|---:|
| 计算机视觉 | 1,304 |
| 多模态学习 | 2,686 |
| 强化学习 | 1,060 |
| 自然语言处理 | 153 |
| 信息检索与 RAG | 369 |
| 安全与对齐 | 553 |

实施脚本为 `backend/scripts/extend_topics.py`。先备份后增量添加，不删除论文、不修改 venue 级别、不覆盖旧方向及人工标签；重复运行幂等。新增方向写入种子文件，后续采集沿用同一规则。

本次备份：`backend/data/backups/before-topic-extension-20260919T124108Z.db`。

本地数据快照：27,238 篇论文、28 个配置来源、24 个已有记录的来源、9 个方向、17,492 篇有通过安全校验的开放版本链接（约 64%）。论文总行数在方向扩展前后未改变。这些是本地记录数，**不是完整 CCF 目录或完整会议论文覆盖率**。

## 4. API 与可靠性改动

- 新增 `GET /api/stats/dashboard`，全部指标来自当前数据库的 SQL 聚合。
- 列表、搜索与统计共用 `PaperFilters`；`level/type/venue/year/access` 之间 AND，多个方向之间 OR，防止多标签重复计数。
- 基础 scope 同时限制论文和 venue 为 A/B，venue 类型为会议或期刊，不将旧 C 类/未知类型脏记录计入界面。
- 卡片新增摘要预览、前三作者、来源类型、官方/OA外链等字段；旧 API 字段保持兼容。
- 统一校验外链：拒绝不安全协议、凭据、控制字符、本地域名和非公网字面 IP；只生成/展示 URL，不请求或下载原文。
- 对安全链接的纯校验结果做有界缓存（32,768 条 URL 组合、1,024 个 host），长键不进入缓存，URL 改变自然重新校验；测试覆盖淘汰、上限和数据库更新。
- 纯标点占位摘要（如单个逗号）在展示与覆盖统计中视为缺失，不修改原始数据库字段。
- 修复原管理页初始化时引用尚未定义的 `status` 导致的运行时错误；历史 running 日志不再无条件冒充活跃任务。
- 当前年份/未回填年份、自动多标签、未核验发表归属均显式提示。前端不以图片或假数字填补缺口。

## 5. 调度与链接模式

- 默认 `PDF_DOWNLOAD_ENABLED=false`，本次启动确认无 PDF 下载任务注册。
- 元数据每周一 04:00；链接补全每周二 06:30；时区显式指定 Asia/Shanghai。
- 旧版每周四高峰重复采集已移除，既有每月一次引用维护保留。
- 图表每 30 秒刷新的是**本地统计**，不会因此触发外部抓取。状态运行中每 3 秒读取、空闲每 60 秒读取。
- 本次界面验收没有触发全库采集或批量下载。

## 6. 验收证据

### 自动化

- 后端：`python -m pytest -o addopts= --disable-warnings` → **178 passed, 1 warning**。警告为既有 Starlette/httpx 测试客户端弃用提示。
- 前端：`npm test` → **22 passed**（3 个测试文件）。覆盖搜索防抖/回车/URL恢复、管理页渲染、图表按钮、卡片外链、分页参数及不安全URL。
- 前端：`npm run build` → **成功**；主包约 232 kB（gzip 约 76.8 kB），页面按路由拆分，不加载 PDF.js。
- Node 22 下升级并验证 Vite 7.3.6、React Router 7.18.4、Vitest 4.1.11。依赖安装时报告 0 vulnerabilities；最终 `npm audit` 复查遇 npm 注册表旧审计端点 HTTP 400，因此**不把最终依赖审计记为通过**。

### 实际浏览器

| 验收动作 | 实测结果 |
|---|---|
| 总览年度图点击 2025 | 跳转 `/papers?year=2025`，显示本地 16,740 篇 |
| 输入 `speculative decoding` | 防抖后 URL 含关键词，2025 范围内 37 篇 |
| 同时切 B 类会议 | URL 加 `level=B&type=conf`，结果缩至 11 篇，与 API 一致 |
| 打开 `Speculative Decoding for Multi-Sample Inference` | 展示完整摘要、元数据、官方和 arXiv 开放链接 |
| 详情返回列表 | 年份、关键词、B类会议筛选全部保留 |
| 采集管理 | 正常显示当前空闲、下次周一/周二运行时间、覆盖卡片和日志；无下载按钮 |
| 会议与期刊 | 来源卡片、A/B分类、数量和微型图正常展示 |
| 390×844 手机来源页 | 单列卡片、折叠导航；文档宽度384px不超过390px视口，无横向溢出 |
| 手机论文页展开筛选 | `aria-expanded=true`、筛选面板可见，布局无横向溢出 |

手机切尺寸时曾截到侧栏过渡动画；稳定状态侧栏在视口外。补充 `visibility/pointer-events` 控制，避免隐藏侧栏仍抢焦点；同一路径方向切换也会收起手机导航。

## 7. 已知边界与未声称完成的事项

1. 现有来源清单未在本次 UI 任务中重新逐条核对 2026 官方公示稿，也未把参考站的全目录复制进来；页面明确使用“当前配置的 A/B 清单”。
2. 本库 2023/2024 当前无数据、4 个配置来源暂无记录；以零值如实展示，不把它说成没有发表。
3. 部分论文归属、主会/分轨、作者名、引用数由旧采集逻辑带入，仍需与官方出版页核验。本次没有冒充已解决所有上游数据质量问题。
4. 官方/OA链接做了安全校验和正确格式化，但没有逐一联网打开 27,238 篇原文来保证全部外链可达。
5. 本次部署验收为本地开发服务器 8000/5180；未执行 Docker 镜像构建与容器部署验收。

相关说明：`README.md` 已更新运行/测试方式；`系统设计方案.md` 的 §4.5 已同步当前界面与交互，不再描述默认下载 PDF。
