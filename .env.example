# Copy to .env; never commit credentials.
CONTACT_EMAIL=
S2_API_KEY=
# Link-only release: no PDF downloads.
PDF_DOWNLOAD_ENABLED=false
SCHEDULER_ENABLED=true
LINKS_MAX_BATCHES=20
WEB_PORT=8080
# Optional TLS SMTP alert settings.
ALERT_EMAIL=
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
