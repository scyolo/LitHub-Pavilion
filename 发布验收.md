# LitHub Pavilion 发布前优化与验收

> 2026-09-21 · 知汇于此，文藏此殿

## 交付范围

本次按“完整优化网站并推送 GitHub”的要求整理运行代码、回归测试、容器部署、CI 和安全发布排除规则。默认保留每周更新、九个研究方向和链接模式；未把本地论文数据库或 PDF 纳入发布。

## 关键修复

| 方面 | 原问题 | 已落实 |
|---|---|---|
| 任务提交与退出 | 创建后无人持有的协程；重叠提交时未等待协程；退出时数据库线程可能尚未完成 | 统一持有 Task，原子占位，支持等待/取消；数据库操作使用独立会话并等待线程收尾 |
| 状态可信度 | 失败单元仍可能把整轮标成功；零收录/不完整回填写完成断点 | 总状态按成功/部分/失败汇总；只为完整单元写 `complete-v2` 断点，旧错误断点不再视为已完成 |
| 数据保护 | 链接回填与删除元数据会删除本地文件 | 不再删除文件；已有归档路径保留；备份不自动清理旧快照 |
| DBLP 适配 | 未处理 `result.hits` 标准响应与单作者对象 | 同时接受 result 包装、单作者/列表形式，检查分页完整性 |
| 来源归属 | S2 外部 key 前缀被当作正式出版年份/主会证据 | 不再提升核验标记；用上游年份和 provenance 标识，主题兜底明确为不完整集合 |
| S2 补全 | 未知/不对齐批次、永久 4xx 被反复重试 | 全未知特例保持全 None，响应长度校验，永久校验错误不重试 |
| OpenAlex | 分页游标重复可持续循环；源解析限流与作品限流语义不同 | 游标重复/上限终止；统一预算/限流异常 |
| 外部请求 | URL/DNS 检查与实际连接之间存在再次解析空间 | 公网 URL 校验、连接时验证全部 DNS 结果并使用数值 IP；保留原 TLS SNI，禁自动重定向和环境代理 |
| 人工 API | arXiv-only 记录无法补录；无效年份可触发任务；方向替换被旧规则并回 | 输入范围校验，规范标识与链接，手工补录不标已核验；方向 PATCH 按当前集合替换 |
| 本地写操作 | 任意浏览器来源可尝试修改本机 API | 拒绝非回环 Origin 和 cross-site 写请求；仍不提供多用户鉴权 |
| 初始化 | 新机器空库启动失败；重导种子覆盖配置；路径依赖工作目录 | 首次自动建库/FTS；已有库检查后保留；新增种子只补缺失，默认路径固定 |
| 前端体积与交互 | 无用 PDF.js 依赖和约 3.3 MB 静态资源；IME 输入中途提交 | 移除未引用 PDF 组件与依赖，构建禁 public vendor 拷贝；组合输入结束后才检索，非法排序归一 |
| 发布运维 | 无真实容器启动证据、无 CI、排除文件不完整 | 双非root只读容器、持久化命名卷、环回端口、nginx头部加固、CI三任务与源码白名单打包 |

## 已执行验证

- 本地完整后端：**221 passed, 1 warning**。警告为原有 Starlette/httpx 测试客户端弃用提示。
- Ruff：`F,E9` 检查全部通过；不宣称跑过所有 lint 规则或类型检查。
- 前端：**26 passed**，生产构建成功。
- `npm audit --omit=dev`：0 vulnerabilities（本次运行结果，仅运行依赖）。
- Docker：后端/前端镜像构建成功；隔离 Compose 项目 `lithub-release-check` 使用独立空命名卷，健康检查通过。
- 空库冒烟：页面品牌、安全响应头、API/FTS、28 个来源、9 个方向、链接模式、空搜索及非法级别 400 全部通过。
- 身份检查：API 容器 UID 1001，nginx UID 101；`nginx -t` 通过。
- 安全传输实测：向一个已知 OpenAlex 元数据端点发出单次 HTTPS 请求，HTTP 200；未执行全库采集。
- 用户已有库继续由本地服务使用，没有删除重建；隔离 Docker 验收未挂载用户库。

## 发布内容

`python scripts/package_source.py` 只选择源码、测试、种子、锁文件、README、安全说明、CI和本报告，并产生带 SHA-256 的文件清单。自动排除 `.env`、私钥常见后缀、数据库、备份、PDF、node_modules、构建输出、工具缓存和历史本地会话文件。源码凭据模式检查不是全面第三方安全认证，仍需提交前逐项核对文件列表。

## GitHub 状态

目标为 `https://github.com/scyolo/LitHub-Pavilion.git`。GitHub CLI 已经用户授权安装并启动浏览器登录；本轮设备授权因 `context deadline exceeded` 超时，`gh auth status` 确认尚未登录。因此还未读取目标仓库历史、未创建远端提交、未强推，CI文件也尚未在 GitHub 执行。登录完成后须先核验账号、仓库和默认分支，再保留远端历史推送。

## 仍需明确的边界

- 当前项目限定配置来源 A/B，并不代表已核验全部 CCF 2026 公示目录或逐篇主会录用信息。旧库的错误归属不会被本轮自动重写。
- 一次采集命中更多论文不是完整覆盖证明；S2 主题词兜底始终标为部分覆盖。
- 桌面本地版本无登录，不能部署到公网。Origin检查不能替代公开部署需要的鉴权。
- PostgreSQL 尚无可直接切换的搜索适配；当前仅支持 SQLite FTS5。
- Python运行依赖固定直接版本，但并非全部传递依赖的哈希锁；未报告未运行的覆盖率百分比。
- 旧文档是历史设计，当前运行和部署约定以 README 与本报告为准。
