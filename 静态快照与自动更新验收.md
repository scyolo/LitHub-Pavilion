# 静态快照与自动更新验收

验收日期：2026-09-24。目标：本地采集、公开快照、GitHub Pages 阅读；后端未启动时不阻断阅读。

## 当前结论

本地实现、生产构建和隔离 Docker 验证已完成。GitHub Pages **尚未上线**：目标仓库 `scyolo/LitHub-Pavilion` 为私有，账号当前为 Free；创建 Pages 的 API 实际返回 HTTP 422：`Your current plan does not support GitHub Pages for this repository.` 未修改仓库可见性，也未冒充部署成功。

当前账号的既有 Git Credential Manager 授权已接入 GitHub CLI 的系统 keyring，账号为 `scyolo`。仓库设置 `PAGES_DEPLOY_ENABLED=false`，以免未配置的发布任务误报。源码与发布管道就绪后，可由仓库所有者选择公开仓库、支持私有 Pages 的套餐或另建公开站点仓库，再启用部署开关。

## 本次补齐

- 本地前端 `auto` 模式在 API 网络失败、超时、5xx 或非 JSON 响应时回退到真实快照；400/404 等业务错误不伪装为旧数据。每 15 秒及聚焦检查后端，恢复后切换查询缓存。
- 公共静态构建只使用快照和 HashRouter，完全不请求访客本地 API。完整快照校验通过后原子切换版本，不混合新旧查询缓存。
- DOI、DBLP 与新旧 arXiv 标识可重建安全原文链接；列表和详情均使用直接外链，无需后端跳转。
- Python / Docker 启动默认收集配置年份与启用的 A/B 来源；先提交采集，不等待慢速发布。定时任务忙时等待，周增量限定在配置范围最后两年。
- 导出内容哈希分片，保留旧版本，拒绝空库覆盖；发布前重校验。大快照准备工作在线程运行。容器导出的公开文件可由不同 UID 的 nginx 读取；后端停止时前端仍可运行、重启及读取快照。
- 发布只写专用 `site-data` 分支，不强推。区分 dispatched 和 deployed，以线上 manifest 验证上线；未上线延时重派发，相同版本不重复建提交。公开页面不带令牌。
- Pages 输入仅复制当前与上一版经过公开 schema 校验的资产，不复制仅有哈希文件名的任意 JSON。
- 增加测试隔离，防止复制 `.env` 后测试误触发采集或发布。源码打包已包含 `frontend/src/data/`、静态目录占位说明和 Pages Compose 配置，仍排除生成数据、数据库与凭据。

## 已执行验证

| 验证项 | 结果 |
|---|---|
| 后端完整回归 | 283 项通过（包括模拟上游的真实 lifespan 自动采集/导出集成测试） |
| 前端完整回归 | 83 项通过（含筛选、搜索、清空、分页重置、详情、外链、回退/恢复、缓存换版） |
| Python Ruff F/E9 | 通过 |
| Vite 静态构建 | 通过，`/LitHub-Pavilion/` 子路径 |
| Docker API/web 镜像构建 | 通过 |
| 隔离 Compose 空库冒烟 | 通过：健康、安全响应头、SQLite/FTS5、28 来源、9 方向、链接模式 |
| nginx 配置与跨容器快照读取 | 通过 |
| 测试后端停止 / web 重启 / API 恢复 | 通过；快照与页面保持可读取 |
| 生产 npm 依赖审计 | 0 漏洞 |
| 真实快照校验与 Pages staging | 通过；原始快照未修改 |
| 公开源码凭据模式检查 | 通过；不等于完整安全审计 |

后端存在一条当前环境的 Starlette/httpx 弃用警告，不影响通过。项目未配置 TypeScript 检查或前端 lint；未报告不存在的检查为通过，也未生成覆盖率。

### 真实数据

- 快照时间：2026-09-22 20:35（Asia/Shanghai）。
- 论文：28,610；58 个论文分片，约 68.5 MB 原始 JSON。
- 官方链接可生成：28,610 / 28,610；有开放版本：18,106；无任何可生成原文链接：0。
- `speculative decoding` 全文词项检索匹配 63 篇；`specdec` 方向标签匹配 57 篇，两者语义不同，不要求同数。
- revision：`010c041eb2e7e48e8c8e0ce6324132b2d1a1abac077288008b91e7d591f482e8`。

链接数字代表安全有效的 URL 构造，不代表已联网遍历每个出版站。没有声称外站永久免费、免登录或永久在线。

## 浏览器验收与证据

使用现有真实快照，8000 端口后端未启动。所有下面图片均已实际查看，位于本地被 Git 忽略的 `artifacts/snapshot-acceptance/`；没有发布含个人账号的截图。

### 1. 无后端总览：通过

实际展示 28,610 篇、28 个来源、9 个方向及 63% 开放链接覆盖，并明确显示正在浏览本地快照。

![无后端总览](artifacts/snapshot-acceptance/t1-local-offline.png)

### 2. Pages 子路径与直接详情：通过

以 `http://127.0.0.1:4174/LitHub-Pavilion/` 预览公开构建。首页正常加载；页面生成的详情链接为 `#/papers/28706`，直接打开后呈现对应论文。

![Pages 总览](artifacts/snapshot-acceptance/t2-pages-overview.png)

![详情深层链接](artifacts/snapshot-acceptance/t3-deep-link.png)

### 3. 深层链接刷新：通过

在详情页执行浏览器刷新，重载完成后仍显示同一论文，无 404；外链属性实际为 `https://doi.org/10.1016/j.patcog.2026.114960`、`target=_blank`、`rel=noopener noreferrer`。

![刷新后的论文详情](artifacts/snapshot-acceptance/t4-detail-reloaded.png)

### 4. 390×844 窄屏详情：通过

标题、作者、摘要及原文按钮不重叠，阅读栏自适应为单列。

![窄屏论文详情](artifacts/snapshot-acceptance/t5-mobile-detail.png)

### 5. 普通链接点击和新窗口外站：工具阻塞，不能认定端到端通过

内置浏览器多次普通 locator 点击超时。方向入口经只读几何核验可见、无覆盖、pointer-events 正常；官方链接点击后，受控和用户标签页列表均未出现新外站。因此保留为未完成验证，不通过注入 click、强制点击或伪造新窗口绕过。组件层点击与链接属性测试通过，不能替代真正的浏览器外站点击验收。

![点击工具超时现场](artifacts/snapshot-acceptance/t6-click-timeout.png)

后续手机管理页的浏览器调用也超时，未计为验收通过。当前浏览器接口未提供可用的控制台日志读取能力，未通过脚本注入监听器补造日志。

### 本地环境问题及处理

首轮 Windows Git Bash 将 `VITE_BASE_PATH=/LitHub-Pavilion/` 转为磁盘路径，且预览基路径与构建不一致，出现一次空白预览。这轮环境测试判为无效；通过直接子进程环境传值和同基路径启动在 4174 重新验收通过。Vite 配置已拒绝磁盘式 base 参数，README 说明 PowerShell 写法。

## 数据与环境保护

- 未修改原始 `backend/data/papers.db`，没有执行真实外网批量采集，没有下载 PDF。
- Docker 验证只使用 `lithub-snapshot-check` 隔离项目，真实启动集成用临时库和模拟上游。
- 隔离容器与网络已 `down`，未使用 `down -v`；测试卷保留。
- 本机 `.env` 与 `.secrets/github_token` 已按用户“配置”要求生成，均被 Git 忽略；Windows 令牌文件权限限制为当前用户。发布仍保持关闭，直至 Pages 资格/可见性决策完成。
- 启动日常 Docker 或 Python 后将默认采集，请用 `STARTUP_CRAWL_ENABLED=false` 显式关闭；关闭调度器不等于关闭启动采集。

具体运行、导出、首次发布和恢复步骤见 [README](README.md)。
